<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
        <meta http-equiv="x-ua-compatible" content="ie=edge" />
        <title>Get CSV data</title>
        <link rel="icon" href="img/webghat-logo.png" type="image/x-icon" />
        <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.15.2/css/all.css" />
        <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700;900&display=swap"/>
        <!-- CSS -->
        <link rel="stylesheet" href="css/mdb.min.css" />
    </head>
    <body>
      <%-include("header")-%>
        <div class="container my-5">
            <div class="row justify-content-center text-center mb-4">
                <h2>Available files are listed below</h2>
            </div>
            <div class="row justify-content-center">
                <div class="col-xl-8 col-lg-8 col-md-10 col-sm-12 col-xs-12 table-responsive">
                    <table class="table table-bordered table-hover align-middle text-center">
                        <thead>
                            <tr>
                                <th scope="col">Upload Date</th>
                                <th scope="col">Industry/Domain</th>
                                <th scope="col">Area/City</th>
                                <th scope="col">Records count</th> <!-- this field will show total record count -->
                                <th scope="col">Download CSV</th>
                            </tr>
                        </thead>
                        <tbody>

                            <%users.forEach(function(user){%>
                               
                            <tr>
                                <th scope="row"><%=user.createdAt.toDateString()%></th>
                                <td><%=user.domain%></td>
                                <td><%=user.area%></td>
                                <td><%=user.totalcount%></td>
                                <td>
                                    <button type="button" class="btn btn-danger btn-sm px-3" onclick="window.location.href='<%=user.link%>'">
                                    <i class="fas fa-arrow-down"></i>
                                    </button>
                                </td>
                            </tr>
                           <%})%>
                            
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        <!-- JS -->
        <script type="text/javascript" src="js/mdb.min.js"></script>
    </body>
</html>